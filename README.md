# AL

Trang giới thiệu của AL, xây dựng bằng Node.js và HTML/CSS/JavaScript thuần.

## Chạy local

```bash
npm start
```

Mở `http://localhost:3000` trong trình duyệt.

Project có cấu trúc:

```text
al/
├── package.json
├── server.js
└── public/
	├── index.html
	├── style.css
	└── app.js
```